/**
 * Props for the UiCoverageFiltersDrawer component (coverage-map filters:
 * trade search + zones to attack + uncovered regions info).
 */
export type UiCoverageFiltersDrawerProps = {
  /** Whether the drawer is open. */
  open: boolean
  /** Whether to show the "back" affordance (drawer stacked on another). */
  showBack: boolean
}
