/** Props of the demo preview frame; `reloadKey` changes reload it, `heightClass` sizes it. */
export type AssistantDemoPreviewCardProps = {
  demoUrl: string
  reloadKey: number
  heightClass: string
}
