/** Props of the business's side of the demo: the SMS notification and the hint under it. */
export type AssistantDemoOwnerFeedProps = {
  timeLabel: string
  assistantName: string
  alertText: string
  isExample: boolean
  hintText: string
  arrivalKey: number
}
