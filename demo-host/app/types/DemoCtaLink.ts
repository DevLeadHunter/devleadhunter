/** Props of the demo pages' black pill link; `pulse` adds the halo and light sweep that nudge the click. */
export type DemoCtaLinkProps = {
  href: string
  pulse: boolean
}

export type DemoCtaLinkEmits = {
  click: []
}
